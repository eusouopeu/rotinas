# CLAUDE.md

Guia de contexto do projeto **Brita** para o Claude Code. Objetivo: evitar que cada sessão precise re-explorar o repositório do zero — leia este arquivo primeiro, e use grep/busca direcionada no `index.html` (~13,6 mil linhas) em vez de carregá-lo inteiro sempre que possível.

> **Números conferidos em 2026-08-22** (atualizar junto com mudanças grandes; ficaram 68% desatualizados uma vez e toda sessão nova calibrava errado o custo de abrir o arquivo): `index.html` ~13.600 linhas e ~600 funções; `app.css` ~1.300 linhas; 41 telas no dispatcher; 12 arquivos de teste em `test/`.

## Localização

`/Users/pedro/Codigos/Apps/Brita`

## O que é

PWA (Progressive Web App) chamado "Rotinas" (nome interno do projeto: Brita): rotinas cronometradas, notas rápidas em markdown, modelos de lista/matriz, e gerenciador de tarefas de estudo — tudo local-first, sem backend.

## Stack e ambiente

- **Sem framework, sem bundler.** HTML + JavaScript vanilla em `index.html` (JS num IIFE **async** no fim do body) + **`app.css`**, um `<link>` comum ao lado de `fonts/fonts.css`. Não introduzir React/Vue/bundlers a menos que explicitamente solicitado.
  - O CSS **saiu do `<style>` inline** em 2026-08 (extração de ~1.300 linhas). Não é build step — é `<link rel="stylesheet" href="app.css">`, carregado por `file://` no Electron e no WebView do APK igual às fontes. **Arquivo novo ao lado precisa entrar em `sw.js` (`ASSETS`) e em `package.json` (`copy:www`)**, senão some no PWA offline e no APK.
  - O **JS continua num IIFE único** de propósito: ~600 funções compartilham o mesmo escopo léxico (as globais `routines`/`notes`/`gam`… e a ordem de declaração que evita TDZ). Quebrá-lo em vários `<script>` exigiria um objeto de namespace e reescrever centenas de referências — é um refactor à parte, não um efeito colateral de outra tarefa.
- **Persistência**: helpers síncronos `load(key, fallback)` / `save(key, val)` / `removeKey(key)` sobre um **espelho em memória** (`mem` Map), com gravação assíncrona enfileirada num backend: **IndexedDB** no navegador, **Capacitor Filesystem** (um JSON por chave em `Directory.Data/brita/`) no APK. Migração automática do localStorage legado na primeira execução. Nunca usar `localStorage` diretamente.
- **APK Android via Capacitor 8** (plugins: filesystem, local-notifications, share). `isNative` distingue os ambientes; código nativo fica em ramos `if(isNative)`. Compilar: `npm run apk` → `android/app/build/outputs/apk/debug/app-debug.apk`. Exige JDK 21 (baixado pelo Gradle via foojay; caminho fixado em `android/gradle.properties`) e SDK em `~/Library/Android/sdk` (`android/local.properties`). Assinatura: `android/brita.keystore` (gitignorado — repo público; senha `brita-rotinas`; **manter cópia fora do repo**, sem ele a assinatura muda e o Android exige reinstalar).
  - **Sempre bumpar `versionCode`/`versionName` em `android/app/build.gradle` antes de gerar um APK que vai para o celular do Pedro.** Com `applicationId` e assinatura estáveis, o instalador do Android trata um `versionCode` igual ao já instalado como "nada para atualizar" e em vários aparelhos só oferece "Abrir" em vez de reinstalar de fato — sintoma: Pedro reinstala e as mudanças não aparecem, mesmo desinstalando antes. Aconteceu de verdade em 2026-07-28. `aapt dump badging <apk> | grep package:` confirma o que ficou empacotado antes de mandar. Há também um selo de build em Configurações → Dados (`BUILD_STAMP`, perto do topo do IIFE) — atualizar a cada build para o Pedro conseguir confirmar visualmente qual versão está rodando.
- **PWA**: `manifest.json` + `sw.js` (stale-while-revalidate; `CACHE_NAME` agora só limpa caches antigos, deploys chegam no reload seguinte sem bump manual).
- **Fontes locais** em `fonts/` (Lato em títulos/h1-h2-h3/headers de tela, Montserrat no resto — woff2, sem rede; máximo 2 famílias, nenhuma monoespaçada — números tabulares usam `font-variant-numeric: tabular-nums` na própria Montserrat, não uma fonte à parte).
- **Rodar localmente**: servir os arquivos estáticos (ex. `python3 -m http.server`). **Testar**: `npm test` (smoke test jsdom + fake-indexeddb: boot, migração, persistência — rodar após qualquer mudança no index.html).
- No APK, exports usam `nativeWriteExport()` → `Documentos/<pasta>/` (+ share sheet); blob/`<a download>`/`window.print()` não funcionam no WebView. Auto-backup nativo a cada 3 dias no mesmo lugar. Notificações de rotina são pré-agendadas como recorrentes do Android (`syncNativeSchedules`). **O nome da pasta é configurável** (`K_DATAFOLDER`, padrão `"Rotinas"`, `dataFolderName()`/`sanitizeFolderName()`) em Configurações → Backup — vale para exports, auto-backup e o espelho markdown (`Documentos/<pasta>/Notas/`). Trocar dispara `migrarPastaDados(antigo, novo)`, que move os arquivos já existentes (raiz + `Notas/`) via `readFile`/`writeFile`/`deleteFile`, best-effort; `obj._mdPath` de notas/modelos não é reescrito na migração — se autocorrige sozinho no próximo `syncMdFile` (a tentativa de apagar o caminho antigo só falha silenciosamente porque o arquivo já foi movido). Só afeta o app nativo — a store de dados viva (`Directory.Data`, que o widget lê direto via `RoutineStore.java`) continua fixa de propósito, mexer nela quebraria o widget.
- `backupData()` (v8) leva `routines, notes, history, templates, snoozes, diario, diaKanban, exercicios, compromissos` — **coleção nova precisa entrar aqui, nos dois handlers de import** (substituir e mesclar) **e em `SYNCED_KEYS`/`applySyncedKey`**, senão o restore perde os dados ou a chave sai do sync em silêncio. **`test/sync-keys.cjs` cobra as três coisas no build** — foi escrito exatamente porque `exercicios`/`compromissos` já ficaram fora do motor do Android sem ninguém notar. Cuidado com TDZ: o auto-backup nativo chama `backupData()` no boot, então tudo que ela referencia tem de estar declarado no topo do IIFE, junto dos outros `load()`.
- **Desktop via Electron** (`electron/`, `mcp-server/`), terceira casca sobre o mesmo `index.html` — ver `PLANO-DESKTOP-ELECTRON.md` para a arquitetura completa. `isDesktop` (`!!window.electronBridge`) é o terceiro branch de `storageBackend`, espelhando o formato do branch `isNative` (um JSON por chave), só que gravado pelo main process via IPC (`electron/main.js`) em vez do Capacitor Filesystem — `~/Library/Application Support/Brita/brita/<key>.json` no macOS. `electron/preload.js` é a única ponte (`contextIsolation:true`, `nodeIntegration:false`): expõe `window.electronBridge.{getAll,set,del}` para o storage e `window.electronBridge.mcp.*` para a tela de Configurações → Integrações. Rodar: `npm run desktop:start` (copia `www/` e abre o Electron). Empacotar: `npm run desktop:build` (electron-builder, config em `package.json` → `build`, saída em `dist-desktop/`).
  - **Servidor MCP embutido** (`mcp-server/server.js`, `@modelcontextprotocol/sdk` v1.x — não a v2 em split-packages, que ainda exige `@hono/node-server` e é ESM-only): Streamable HTTP em `127.0.0.1:<porta>` (padrão 8765), token gerado no primeiro boot e guardado em `mcp-config.json` (`userData`, fora do repo). Toggle **desligado/somente leitura/leitura e escrita** em Configurações → Integrações, padrão somente leitura. As tools (`list_routines`, `create_task`, `append_diario` etc. — ver seção 4.2 do plano) **não reimplementam lógica de negócio**: cada uma delega para `window.__britaMCP` no `index.html` (bloco `if(isDesktop){...}` perto do fim do IIFE, depois de `startScheduler()`), que chama as mesmas funções que a UI usa (`concludeTarefa`, `registrarConclusao`, `saveDiario` etc.) via IPC (`mcp:call`/`mcp:result` entre `main.js` e o renderer). **Toda tool nova de escrita precisa reaproveitar uma função existente do app, nunca duplicar a regra de pontuação/gamificação.** O card de Configurações → Integrações (`mcpCardHtml`/`refreshMcpCard`, perto de `renderSettings`) atualiza só a si mesmo via IPC, sem passar por `render()` — evita perder o resto do formulário a cada refresh do log de chamadas.
  - **Notificações desktop**: reaproveita o caminho "navegador" (`isNative` falso) que já existe em `showRichNotification`/`notifyTarefaTimeUp`/`dispararNudge` — não é um sistema novo. A única mudança foi um branch `isDesktop` em cada uma das três, chamando `notifyDesktop(title, body, onClick)` (perto de `requestNotifPermission`) em vez do caminho por `navigator.serviceWorker`. Isso é necessário porque `serviceWorker.ready` **nunca resolve** no Electron (carregado por `file://`, o registro do `sw.js` falha silenciosamente) — sem o branch, a notificação travaria para sempre esperando essa Promise. `new Notification()` já mapeia direto pra notificação nativa do SO no Chromium do Electron, sem precisar de `node-notifier` nem de IPC; o clique nela chama `electronBridge.showWindow()` (IPC `app:show` no `main.js`) pra trazer a janela de volta se estiver escondida na tray. `startScheduler()`/`checkSchedules()` (poll a cada 20s) já rodam para o desktop sem mudança nenhuma — continuam ativos mesmo com a janela escondida, porque o renderer nunca é destruído (`close` só esconde, ver `mainWindow.on("close")`).
  - **Sync com Google Drive** (`sync/`), seção 5.3 do plano. Roda só no main process, `setInterval` de 10min (`SYNC_INTERVAL_MS` em `main.js`) mais um sync inicial 15s após abrir e um botão "sincronizar agora". **Desvio deliberado do plano**: não existe `manifest.json` próprio no Drive — o `modifiedTime` que o próprio `files.list` já devolve por arquivo substitui essa segunda fonte de verdade. O que precisa ficar de estado local (pra saber "o que mudou desde o último sync" e diferenciar de conflito de verdade) mora em `sync-state.json` (`userData`, nunca sobe pro Drive).
    - **Merge granular item-a-item** (`merge3Way`/`merge3WayArray`/`merge3WayMap` em `sync/engine.js`, espelhado em `SyncEngine.java` no Android): quando os dois lados mudaram desde o último sync, NÃO é automaticamente um conflito — só é se o MESMO item (id estável, coleções em array) ou a MESMA chave (período, no mapa do diário) foi editado dos dois lados para coisas diferentes. Compara contra `baseContent` (uma cópia do conteúdo salva em `state.keys[key]`/`prev` a cada upload/download/merge bem-sucedido — o "meio" do 3-way merge) e resolve sozinho toda a parte estrutural: item adicionado só de um lado entra, removido só de um lado some, editado só de um lado prevalece. Escalares (tema/weekstart) não passam por aqui — `merge3Way` devolve "não deu" pra qualquer formato que não seja array-de-objetos-com-id ou objeto chave→valor, caindo no fluxo manual de sempre (grava `.conflict-*.json`, pede escolha). Instalação de antes desta função existir não tem `baseContent` salvo ainda: o primeiro conflito depois de atualizar ainda cai no manual, e a base é preenchida a partir daí (write/skip já gravam `baseContent` a cada ciclo). Testado em `test/sync-merge.cjs` (algoritmo puro) e `test/sync.cjs` (integração com `syncOnce`).
    - As **chaves sincronizadas** (`sync/engine.js`, `SYNCED_KEYS`) são as de `backupData()` mais `theme`/`weekstart` (preferência de UI, ver nota do OAuth abaixo). O motor do Android (`SyncEngine.java`, mesma constante) é mantido em paralelo **manualmente** — já divergiu uma vez (`exercicios`/`compromissos` entraram no desktop e ficaram de fora do Android). **`test/sync-keys.cjs` agora barra essa divergência no `npm test`**: lê as três fontes (o array JS, o array Java e o `switch` de `applySyncedKey`) direto do código-fonte e exige que batam. `getStatus()` também devolve `keys:[{key,syncedAt,conflito}]`, que alimenta o painel "Saúde do sync" em Configurações → Backup — o espelho em tempo de execução do mesmo teste, para uma instalação que já está torta.
    - **OAuth loopback** (`sync/google-drive.js`): sobe um `http.createServer` numa porta livre em 127.0.0.1, abre o navegador padrão via `shell.openExternal` (nunca webview embutida), PKCE S256. **Client ID/Secret embutidos no repo** (`sync/oauth-client.js`) — mesmo truque do Remotely Save com o Dropbox: um único OAuth Client tipo "App para computador", cadastrado uma vez no Google Cloud Console do Pedro, distribuído junto do código aberto. Client secret de app instalado não é tratado como segredo de verdade pelo próprio Google (documentado — não dá pra manter confidencial num binário distribuído); a tela de consentimento OAuth desse projeto fica em modo "Testing" com só o e-mail do Pedro como testador, o que barra qualquer autenticação de quem não estiver nessa lista mesmo tendo a credencial em mãos. `sync/engine.js`'s `loadClientCreds()` só cai pra essa credencial embutida se não achar um override em `google-oauth-client.json` (`userData`, válvula de escape manual, não exposta na UI). A tela de Configurações → Backup → Sincronização com nuvem só mostra o formulário de colar credenciais nesse cenário de fallback (override ausente/inválido) — com a credencial embutida real, vai direto pro botão "Conectar".
    - **Tokens** ficam em `google-drive-tokens.enc`, criptografados com `safeStorage` (`sync/token-store.js`) — nunca em texto puro, nunca `keytar` (descontinuado).
    - **Conflito de verdade** (os dois lados mudaram desde o último sync): não aplica nada automaticamente — grava o remoto como `<chave>.conflict-<timestamp>.json` ao lado do arquivo real (não sobrescreve), e a resolução é manual em Configurações (botões "manter local"/"usar remoto", que chamam `resolveConflict` — **não** reaproveita o modal de import de backup/`oferecerImportarBackup`, porque aquele foi desenhado pra "mesclar ou substituir tudo" de um arquivo inteiro e não oferece a escolha por lado que um conflito de uma chave só precisa).
    - Quando o motor baixa uma chave (fora de conflito), grava direto em disco e manda pro renderer via IPC (`sync:applied`); `applySyncedKey(key, value)` no `index.html` troca a variável global inteira (`routines = value`, etc.) e chama o `save()` de sempre — é sempre substituição da chave inteira, igual ao branch "Substituir tudo" do import.
    - **Android também sincroniza** (`android/app/src/main/java/com/pedro/rotinas/{DriveSyncPlugin,SyncEngine,GoogleDriveAuth,GoogleDriveApi,SyncTokenStore}.java`), tradução direta do motor do desktop — mesmo algoritmo last-write-wins por chave, mesma pasta `brita-sync` no Drive, mesmo Client ID/Secret embutido (loopback OAuth funciona igual no Android: o client tipo "App para computador" aceita qualquer `http://127.0.0.1:<porta>/` em tempo de requisição, então nenhum cadastro novo no Google Cloud Console foi necessário). O que muda é só onde cada coisa mora: chaves em `Directory.Data/brita/<chave>.json` (mesmo caminho do `storageBackend` nativo), tokens/estado em `EncryptedSharedPreferences` (Android Keystore) em vez de `safeStorage`. A UI de Configurações → Backup é a mesma tela para os dois (`syncBridge()` no `index.html` abstrai `window.electronBridge.sync` vs. `window.Capacitor.Plugins.DriveSync`). Sem `WorkManager`/tarefa periódica de verdade — sincroniza ao voltar pro app (`tentarAutoSyncNative`, chamada de `marcarSegundoPlano`) e no botão manual, nunca com o app fechado; ciclo completo desktop⇄nuvem⇄celular funciona, mas o celular só fala com a nuvem quando o app está aberto.

## Estrutura de arquivos

```
Brita/
  index.html        # HTML template strings + JS (IIFE único)
  app.css            # folha de estilo do app (extraída do <style> inline em 2026-08)
  manifest.json      # metadata do PWA, ícones, atalhos (nova nota / iniciar rotina)
  sw.js               # service worker (cache offline stale-while-revalidate)
  fonts/              # woff2 locais + fonts.css
  test/smoke.cjs      # smoke test; gamificacao/metas/kanban/diario/insights/editor/
                     # datafolder/sync/sync-merge/sync-keys/streak/agendamento-intervalo (npm test)
  package.json        # scripts: copy:www, sync, apk, test — deps só do Capacitor/testes
  capacitor.config.json
  android/            # projeto nativo gerado (build.gradle do app tem a assinatura)
  electron/           # casca desktop: main.js (storage + tray + boot do MCP/sync), preload.js (contextBridge)
  mcp-server/         # servidor MCP embutido (Streamable HTTP), tools delegam para window.__britaMCP no index.html
  sync/               # motor de sync com Google Drive: engine.js (algoritmo), google-drive.js (OAuth + REST), token-store.js (safeStorage)
  assets/             # fontes dos ícones p/ npx @capacitor/assets generate --android
  icon-192.png / icon-512.png / icon-512-maskable.png
```

Não há separação em componentes/arquivos — é intencional (app pequeno, single-file). Ao editar, localizar a seção certa por `grep -n` antes de abrir o arquivo inteiro.

## webapp/ — reescrita em React + TypeScript (EM ANDAMENTO desde 2026-08-29)

Pedido explícito do Pedro: reescrever o app inteiro em React+TypeScript com Vite, mantendo compatibilidade com deploy no GitHub Pages. Dado o tamanho (41 telas, ~600 funções em `index.html`), a migração está sendo feita em fases — **não tratar como concluída nem tentar "terminar de uma vez"**; cada sessão porta mais uma tela/módulo seguindo o padrão já estabelecido.

**Status resumido (2026-08-29):** 10 de 41 telas existem no React, todas parciais — Home, Settings, RoutineEditor, RoutineDetail, Player, Done, Metas, Diario, Notes, NoteEditor (detalhe tela a tela no bullet "Telas portadas até agora" abaixo). A lógica mais delicada (gamificação/pontuação de rotinas e metas) já está portada e testada de verdade (`lib/gamificacao.ts`, `lib/scoring.ts`, `lib/metas.ts`), não é placeholder. A tabbar já tem as 5 abas reais (Rotinas, Metas, Diário, Modelos, Ajustes) — a aba Modelos abre direto em Notas, igual ao app antigo. **Busca global** existe (`components/GlobalSearch.tsx`) — overlay com filtro tudo/rotinas/metas/notas + peso, aberto pelo botão da `DesktopTopbar` ou pelo atalho `/` (guardado contra digitação em curso); sem filtro de área, sem "modelos"/"kanban"/"histórico" (telas de destino ainda não existem) e sem gatilho na home mobile ainda (só desktop + atalho). **Ainda não dá pra usar no dia a dia** — faltam ~32 telas (os 9 tipos de Modelos além de Notas, busca global, backup/importação, sync com Google Drive, MCP, PIN, atalhos de teclado) e duas peças de risco técnico maior ainda não enfrentadas: o editor markdown "live" (Notas/Diário) e a consolidação do drag-and-drop. O app antigo (`index.html`) continua sendo o que roda no celular/desktop do Pedro, intocado.

- **Convivência com o app antigo**: `index.html`/`app.css`/`sw.js`/`manifest.json` na raiz **continuam intocados** e são o que `npm run apk`/`npm run desktop:*` empacotam — é o app que o Pedro usa todo dia no celular/desktop. O app novo mora isolado em `webapp/` (raiz do projeto Vite: `webapp/index.html` mínimo + `webapp/src/`), builda para `webapp-dist/` (gitignored). Só quando a paridade de telas + lógica + pontes nativas for atingida (fase futura) acontece um corte único: `capacitor.config.json.webDir` e `electron/main.js` passam a apontar pra build nova, e `index.html`/`app.css`/os testes `.cjs` obsoletos saem.
- **Toolchain**: `vite.config.mts` na raiz (`root:"webapp"`, `base:"./"` — relativo, funciona em GitHub Pages num subpath de projeto, em `file://` do Electron e no `webDir` do Capacitor sem configuração por ambiente; `publicDir` aponta pra `fonts/` da raiz em vez de duplicar os `.woff2`). `tsconfig.json` com `strict:true`. Scripts: `npm run dev:react` (vite), `npm run build:react` (saída em `webapp-dist/`), `npm run test:react` (vitest), `npm run typecheck:react` (tsc). `.claude/launch.json` tem duas configs de preview: `brita` (servidor estático do app antigo, porta 8123) e `brita-react` (`npm run dev:react`, porta 5173) — usar a segunda pra ver o React no navegador. `npm test` já roda a suíte `.cjs` antiga **e** `test:react` em sequência.
- **Estado**: uma store Zustand única (`webapp/src/store/useAppStore.ts`), mesmo padrão save(K_X,x) do app antigo por baixo (`webapp/src/lib/storage.ts` porta `load/save/removeKey` + os 3 backends — Electron/Capacitor/IndexedDB — quase literalmente, **mesmo formato de arquivo/registro que o app antigo já lê/escreve**, não é um storage novo). `webapp/src/lib/constants.ts` tem as chaves `K_*`. `webapp/src/lib/gamificacao.ts` porta o núcleo PURO do motor de pontuação (o que não depende de `routines`/agendamento, que ainda não foi portado — ver comentário no topo do arquivo).
- **CSS**: `app.css` da raiz é importado como está (`main.tsx`), sem migrar pra CSS Modules/Tailwind — className dos componentes novos usa os mesmos seletores já existentes.
- **Ícones**: `webapp/src/lib/icons.ts` porta `ICON_D` literal; `components/Icon.tsx` é o `svgIcon()` de sempre como componente.
- **Telas portadas até agora**: `Home` (parcial — lista de rotinas + criar/editar/excluir via o `RoutineEditor` de verdade + ver detalhe via `RoutineDetail` + iniciar via `Player`; faltam agenda inline, card da roda da vida, avisos, swipe-to-delete), `RoutineDetail` (parcial — nome/ícone com cor da área da roda (`lib/scoring.ts:corDaRotina`), meta de etapas/duração, chip de horário+dias (`lib/schedule.ts:diasChipLabel`), lista de etapas só leitura, Editar/Começar; faltam reordenar etapa por arrastar, editar etapa direto daqui, duplicar/excluir etapa por swipe — **cuidado**: reaproveita a classe `.routine-list`, que vira grid de 2 colunas em telas ≥900px — por isso força `display:flex;flex-direction:column` inline, já que ainda não existe a variante split do desktop `.home-split-detail` do app antigo), `Settings` (parcial — Aparência, Início da semana, Notificações, Roda da vida, Hábito consolidado, Vagas por peso, Pontuação do boletim; faltam PIN, mensagens motivacionais, overlay do cronômetro, sync, MCP, dados/backup, calendário externo, mini player, atalhos, e a simulação "e se" da pontuação), `RoutineEditor` (parcial — nome, etapas só tipo "tempo" sem reordenar por arrastar, agendamento por dias+horário com preview computado via `lib/schedule.ts`; faltam peso no boletim, área da roda, hábito, nota anexada, meta semanal, modo "a cada N dias", tipos checklist/exercício), `Player` (parcial — só etapas "tempo", contagem regressiva com pausa/retomar, anterior/concluir, Wake Lock, **e agora grava K_HISTORY e credita pontos de verdade** via `lib/scoring.ts`, incluindo estorno ao voltar etapa; faltam modo zen, etapa exercício/checklist, anotações, nota vinculada, adiar/não-fazer, painel de etapas do desktop), `Done` (minimalista, sem resumo de pontos/streak/badges), `Metas` (parcial — só "Prazos": criar/editar/excluir, progresso com pontuação proporcional de verdade via `lib/metas.ts`; faltam Recorrentes, sub-metas, áreas da roda, filtro "só hoje", PDF) `Diario` (parcial — texto simples por período dia/semana/mês/ano; falta agenda/time-blocking, kanban por período, calendário mensal) e `Notes`+`NoteEditor` (parcial — lista com busca/filtro por tag/pin/excluir, editor com título/assuntos (input livre, sem sugestões)/conteúdo; falta editor contínuo "live preview", backlinks `[[nota]]`, sinkChecked, espelho markdown nativo e paginação "carregar mais"). As outras ~32 telas (incluindo os 9 sub-renderers de `renderTemplateDoc`) ainda não existem no React.
- **Motor de pontuação ligado a rotinas** (`lib/scoring.ts`, extensão de `lib/gamificacao.ts` que depende de `Routine`): `construirAgendaSemana`/`congelarSemana`/`avancarGamificacaoAteAgora`/`registrarConclusaoStep`/`desfazerConclusao`, todas puras (devolvem `gam` novo em vez de mutar+`save()`), testadas em `lib/scoring.test.ts` com os mesmos números batendo (ex.: pesoBruto × fator de normalização). **Simplificação desta fase**: o desconto de "hábito consolidado" sempre usa `mapaHab={}` (nenhuma rotina descontada) porque `computeStreakFor` ainda não foi portado — não é regressão, só significa que o desconto de hábito ainda não é aplicado (rotina vale o peso cheio sempre). `boot()` chama `avancarGamificacaoAteAgora` para fechar semanas/meses/trimestres/anos pendentes, igual ao app antigo.
- **Riscos conhecidos, ainda não enfrentados**: o editor markdown "live" (`liveAplicar`/`liveColocarCursor` no app antigo) briga com o modelo de reconciliação do React — vai exigir uma fase própria. Drag-and-drop é Pointer Events custom (~6 implementações duplicadas no app antigo) — vira um hook único ao ser portado, não 6 cópias. A ponte MCP (`britaMCP`) só faz sentido religar depois que a maior parte do resto existir.
- **`templates` (K_TEMPLATES) é tratado como passthrough genérico** (`AnyTemplateDoc` em `useAppStore.ts`): só o doc `type:"countdown"` (Metas) é lido/escrito de forma tipada — todo outro tipo de documento (mercado, kanban, matriz, etc., que o app antigo já pode ter criado) passa por como está, sem risco de perda quando isto um dia convergir com os dados reais do Pedro.
- **`webapp/index.html` usa `<div id="app">`, não `#root`** — `app.css` tem regras de layout críticas (`#app{ height:100vh; display:flex; flex-direction:column; ... }`, escala de fonte via `transform`) escritas para esse id especificamente. Usar `#root` (padrão do Vite) faz `.screen`/`.tab-scroll` (`flex:1`) nunca resolverem uma altura real — todo o app "encolhe" pro tamanho do conteúdo em vez de preencher a tela. Se algum dia recriar o entry point do zero, manter o id `app`.
- **Bug conhecido, PRÉ-EXISTENTE em `app.css` (não é do React port)**: em `@media (min-width:900px)`, `.home-header{ top:var(--topbar-h) }` deixa o cabeçalho sticky "grudado" 60px abaixo do topo mesmo sem rolar (comportamento correto de `position:sticky` quando a posição estática do elemento, por ser o 1º filho de `.tab-scroll`, é menor que o offset exigido) — sobrepõe os ~60px seguintes de conteúdo. Reproduzido e confirmado também no app vanilla (`index.html`), só passa despercebido porque o conteúdo visível de verdade normalmente começa depois dessa zona. Flagrado como tarefa separada (`task_75ca4d50`); não corrigido ainda porque mexe em CSS de produção compartilhada. O `DesktopTopbar` do React (`components/DesktopTopbar.tsx`) já existe e resolve a AUSÊNCIA de topbar — o overlap em si é outro bug, upstream.
- **GitHub Pages**: workflow em `.github/workflows/deploy-react.yml` builda e publica `webapp-dist/`. Precisa do Pages habilitado em Settings → Pages → Source: "GitHub Actions" — **não habilitado ainda**, combinar com o Pedro antes do primeiro deploy real (é a única parte pública desta migração).
- **Testes**: `webapp/src/**/*.test.ts` (Vitest) — import direto dos módulos reais, não a técnica de regex-extraction + `vm.runInContext` que os testes `.cjs` do app antigo usam (essa técnica só funciona em arquivo único; quebra assim que o código vira múltiplos módulos TS).

## Padrão de arquitetura (importante — ler antes de editar)

- **Estado de navegação**: uma variável global `view = { tab, screen, id? }`. Trocar de tela = reatribuir `view` e chamar `render()`.
- **Dispatcher central**: a função `render()` (linha ~1021) lê `view.screen` e chama a função `renderX()` correspondente (`renderHome`, `renderTemplates`, `renderTarefas`, `renderRoutineDetail`, etc.). Para adicionar uma tela nova: criar `renderNovaTela()` e adicionar um `else if` no dispatcher.
- **Renderização**: cada `renderX()` monta um elemento via template string (`el.innerHTML = ...`) e registra os handlers (`.onclick`, `.oninput`) depois de inserir no DOM. Não há virtual DOM — é *replace* do conteúdo da tela a cada render.
- **Dados**: arrays globais carregados via `load()` no boot (`routines`, `notes`, `templates`, `tarefas`, `history`, etc.), com uma constante `K_*` por chave (`K_ROUTINES`, `K_TEMPLATES`, `K_TAREFAS`...). Qualquer alteração de dado é seguida de `save(K_X, x)` (ou o helper específico, ex. `saveTemplates(doc)`). O boot aguarda `storageBackend.getAll()` no topo do IIFE async antes de qualquer render.
- **IDs**: gerados com `uid()` (string aleatória base36), nunca incrementais.
- **Gamificação (boletim)**: `K_GAMIFICACAO` guarda `config` (multiplicadores editáveis em Configurações), `semanaAtual` (fator de normalização **congelado** no domingo — não recalcular durante a semana), `historico` e `badges`. O fator vem de `fatorNormalizacaoPara(totalBruto)`: sem nenhuma etapa agendada ele **não pode ser 0** (zerava toda a pontuação da semana, inclusive tarefas de estudo) e cai numa escala padrão de 20 blocos médios de 30 min = 100. Cada conclusão guarda o peso bruto em `pb`, o que permite `repararFatorSemanaAtual()` reescalar semanas antigas salvas com fator 0. Pontos são creditados **etapa a etapa** em `pontuarEtapaConcluida()`, nunca em lote no fim — é o que faz conclusão parcial valer. Viradas de semana/mês/ano são resolvidas no boot por `avancarGamificacaoAteAgora()`, porque o app pode ficar dias fechado.
- **O dia de início da semana é configurável** (`K_WEEKSTART`, `weekStartDow()`, padrão domingo=0). `inicioSemanaISO(date)` devolve o dia configurado que abre a semana da data dada — **nunca** recalcular "início da semana" com `d.getDate()-d.getDay()` solto em outro lugar, sempre passar por `inicioSemanaISO`/`isoToDate(inicioSemanaISO(...))`, senão essa parte da tela ignora a configuração. Existem **duas convenções de "dia" diferentes** e é fácil confundir: `diaSemanaHoje()`/`Date.getDay()` são **literais** (0=domingo..6=sábado, para casar com `schedule.days` da rotina, que é sempre literal); `agendaCongelada[].dia`, `esperadoAte`, `construirAgendaSemana` e as chaves `"kanban:..."` usam **offset dentro da semana configurada** (0=primeiro dia configurado..6=último) — a conversão entre as duas é sempre `offsetSemana(diaLiteral)`. `ordemDiasSemana()` devolve os 7 dias literais na ordem de exibição (gráficos, calendário mensal, agenda semanal do Diário) — usar para reordenar colunas em vez de assumir domingo primeiro. A semana conta para o mês em que **termina** (`anoMesDoFimDaSemana`). Trocar o dia em Configurações reancora a semana em curso na hora via `reancorarSemanaAtual` (preserva o que já foi concluído dentro da nova janela, descarta o resto, reescala pelo novo fator — mesmo formato de `congelarSemana`/`repararFatorSemanaAtual`, todos passando pelo helper `fatorParaArea`). Havia uma base segunda-feira fixa antes: duas migrações de boot cuidam do legado — `migrarSemanaParaDomingo` (reancora `semanaAtual` uma única vez, com o flag `gam.semanaMigradaDomingo` — **sem esse flag**, configurar um início diferente de domingo faria essa migração achar de novo que `inicioISO` está "errado" e desfazer a preferência do Pedro a cada boot) e `migrarSemanasDoDiario` (chaves `semana:<segunda>` viram `semana:<domingo anterior>`).
- **Peso "nenhum"**: quarta opção de `tagValor` (além de baixo/médio/alto) com multiplicador fixo 0 — `gam.config.multiplicadores.nenhum` é reescrito para 0 no boot e não aparece em Configurações. `pesoBruto` devolve 0, então a etapa some da agenda congelada e `registrarConclusao` devolve `null`: a rotina roda normalmente e não pontua. `distribuicaoTags()` deixa essas etapas fora do `total`.
- **Roda da vida** (`gam.config.roda = {ativa, areas:[{id,label,color,peso}], pesoSemArea}`): com ela **ativa**, os 100 pontos da semana são repartidos primeiro ENTRE as áreas (proporcional ao `peso` de cada uma) e só depois entre as rotinas de cada área — é o que tira rotinas de áreas diferentes da mesma disputa. `fatoresPorArea()` devolve um fator por área; área **sem nada agendado não reserva fatia**, senão a semana não fecharia 100. A rotina aponta a área em `r.eixo`, e as áreas são **semeadas dos `EIXOS`** com os mesmos ids — assim a tarefa de estudo (que já tem `m.eixo`) cai na mesma área sem migração. Cada entrada de `concluidos` guarda `area`, o que alimenta `pontosPorAreaSemana()` (seção do boletim) e o reescalonamento de `repararFatorSemanaAtual`. Desligada, `fatoresPorArea` devolve `{}` e tudo volta ao fator único.
- **Hábito consolidado** (`gam.config.habito = {ativo, streakMin, fator}`): rotina com streak ≥ `streakMin` passa a valer `fator` (padrão 0,6) do peso, abrindo espaço para o que ainda não pegou. O estado é decidido **uma vez, no congelamento de domingo**, e guardado em `semanaAtual.habitos` — se dependesse do streak vivo, o peso das etapas mudaria no meio da semana e a agenda congelada deixaria de bater. `construirAgendaSemana(mapaHab)` recebe o mapa por parâmetro justamente por isso (congelar passa um novo, reparar passa o congelado). Opt-out por rotina em `r.semHabito`.
- **Vagas excludentes de peso** (`gam.config.vagas = {alto, medio, baixo}`, 0 = sem limite): quantas rotinas podem ocupar cada nível — **dentro da área** quando a roda está ativa, global quando não. O limite é cobrado **no salvar** (`validarVagaPeso`), não no clique da pílula: rebaixar outra rotina por causa de uma edição que ainda pode ser cancelada deixaria o dado dela errado sem nada ter sido salvo. No editor, `vagaHintHtml()` só informa quem ocupa. Toda essa lógica (roda, hábito, vagas) é coberta por `test/gamificacao.cjs`.
- **Markdown**: `renderMdBlock(texto, chave, msgVazio)` é o renderizador de leitura (negrito, listas, checklist clicável, toggle), usado no overlay de nota e no journaling. Não criar outro parser.
- **Editor contínuo (live preview)**: `renderLiveEditor()` é a superfície de escrita das notas — um único `contenteditable` (`#liveEditor`) em que toda linha aparece formatada, menos a do cursor, que mostra o markdown cru. Modelo do Obsidian. Regras: o texto é a fonte da verdade; **digitação comum é nativa** na linha crua (não interceptar — quebraria o IME/autocorreção do Android) e o `input` só relê essa linha; **edições estruturais são interceptadas** no `keydown` (Enter, Backspace no col 0, Delete no fim, seleção multi-linha) e no `beforeinput` (rede para teclados que só emitem `inputType`), sempre via `liveAplicar(a, b, texto)`. Redesenhar o `innerHTML` destrói o nó focado, então `liveColocarCursor()` **precisa** devolver o foco no mesmo ciclo. A fonte do texto é plugável por `liveCtx` (`noteLiveCtx()` para Notas simples, `diarioLiveCtx()` para o Diário) — só existe uma instância por vez. As anotações de journaling ainda usam o lápis + textarea porque exigiriam várias instâncias simultâneas. Lógica coberta por `test/editor.cjs`.
  - **Barra de atalhos markdown** (`.md-toolbar`, `MD_TOOLBAR_BOTOES`, `ensureMdToolbar`): B/I/título/lista/lista numerada/checklist/recuo±, fixa acima do teclado virtual (posição por `visualViewport`), visível só com `liveFocado`. **A ação roda no `pointerdown`, nunca no `click`** — o `preventDefault` necessário para não tirar o foco do editor cancela os eventos de compatibilidade que o navegador sintetiza no toque, inclusive o `click`; por isso os botões funcionavam no desktop (onde o `mousedown` não cancela o click) e **não funcionavam no celular**. Escondida no desktop, onde existe teclado físico.
  - **Indentação**: `INDENT_PASSO` (2 espaços) + `nivelIndent()`/`partesIndent()`, junto das constantes de parsing (não lá embaixo na barra — `renderMdBlock` e `renderLiveEditor` usam `nivelIndent` e ficariam em TDZ). O recuo vira `margin-left` **na linha crua também, com o mesmo valor**: recuar só a formatada faria o texto pular de lado quando o cursor entrasse na linha. Os botões de lista/título preservam o recuo via `partesIndent` — um `.trim()` solto devolveria a linha para a margem.
  - **A linha do cursor não pode virar uma caixa.** Ela já teve fundo próprio, barra lateral de acento e padding extra, e o título caía de 24px para 15px ao entrar em edição: o texto pulava e o editor "parecia" trabalhar linha a linha em vez de ser um texto corrido — foi reclamação explícita do Pedro. Hoje `.live-raw` é transparente e `classeCruaDaLinha()` devolve à linha crua a tipografia do bloco (`.live-raw-h1/h2/h3`, `.live-raw-li`), então nada muda de tamanho nem de posição quando o cursor entra e sai. Não reintroduzir realce de caixa.
  - A linha crua **precisa continuar sendo um único nó de texto** (`div.textContent = raw`): `liveColocarCursor()` posiciona o cursor em `div.firstChild`. Por isso o destaque da sintaxe é feito por CSS na div, e não embrulhando pedaços em `<span>`.
- **Metas** (aba própria, antes "Contagens regressivas" em Modelos): os dados continuam num doc `type:"countdown"` para não exigir migração — só a apresentação mudou. O escopo vem do prazo (`metaEscopo`: <30 dias mensal, 30–90 trimestral, >90 anual) e o bônus vai para `gam.metasPontos[periodo]`, somado à nota do período no fechamento. A aba **Tarefas** saiu da tabbar para dar lugar a Metas e agora é acessada pelo ícone ☰ no header da home.
  - **Prazos vs. Recorrentes**: toggle multiselect (`metasSubviewSel`, `K_METASSUBVIEWSEL`) — os dois podem ficar visíveis juntos (`ambos`), nunca os dois desmarcados. **Recorrentes é a primeira opção e o padrão** para quem nunca tocou o toggle (fallback em `metasSubviewSel`); Prazos vem depois.
  - **Metas recorrentes** (`doc.recorrentes`, hábitos que repetem N vezes ao dia/semana — ver `metaRecPeriodoAtual`/`metaRecProgresso`) podem ser **negativas** (`rec.negativa`, ex.: "delivery no máx. 2x/semana"): `rec.vezes` vira o LIMITE em vez do alvo, e cada marcação além do limite (`metaRecExcesso`) credita uma penalidade **negativa** na semana corrente (`sincronizarPenalidadeMetaRec` empurra/remove entradas em `gam.semanaAtual.concluidos` com `tipo:"metaRecNeg"`, itemId estável `metarecneg:<recId>:<periodo>:<n>` para poder estornar ao desmarcar). O peso da penalidade é `rec.tagValor` (nenhum/baixo/médio/alto, padrão médio) via `pesoBruto` — mesma "unidade de bloco" do cartão do kanban, com o sinal invertido. `notaSemanaAtual()` é só um `reduce` de `pontos`, então soma automaticamente sem precisar de tratamento especial; o clamp de exibição (`Math.max(0, ...)` na barra do boletim) já existia. Editar o limite/peso ou desligar `negativa` reconcilia via excesso-antes/excesso-depois no salvar; apagar a meta estorna tudo (`estornarPenalidadesMetaRec`). Metas recorrentes **positivas continuam sem pontuar** — são só hábito/lembrete, como sempre foram.
- **Peso da meta**: `t.tagValor` (nenhum/baixo/médio/alto), padrão **alto**, multiplica a base do escopo — `metaPontosTotais` = `pontosMeta[escopo] × tagMultiplicador(peso)`. O padrão é alto de propósito: uma meta tem de pesar mais que uma etapa de rotina (padrão médio) e que um cartão do kanban do dia (padrão baixo). `reconciliarPesoDasMetas` roda no boot, estampa o peso nas metas antigas e acerta o crédito **em silêncio** (`sincronizarPontosMeta(t, true)`) — sem isso uma meta antiga ficaria mostrando "8 de 30 pts" até alguém tocar nela. As **áreas pré-prontas saíram** do formulário (`metaAreasPool` não sugere mais os `EIXOS`); `metaAreaInfo` continua resolvendo id de eixo para metas antigas.
- **Pontuação das metas é proporcional, item por item** (não mais tudo-ou-nada ao concluir): `metaPontosDevidos` = `metaPontosTotais × done/topics`, e `sincronizarPontosMeta` credita só o **delta** contra o que já foi pago. O crédito vai sempre para o período **corrente**, então uma meta que atravessa a virada do mês deixa em cada mês o que foi feito nele — por isso o razão é um mapa por período (`t.creditos = {periodo: pontos}`) e não um número só. Estorno maior do que o período corrente comporta desce para os anteriores (`aplicarDeltaMeta`), para `t.creditos` e `gam.metasPontos` nunca divergirem. `migrarCreditoMeta` converte metas pontuadas pela versão antiga (`t.pontuadaEm/pontuadaPeriodo/pontuadaPontos`) sem pontuar de novo.
- **Gerenciador de tarefas de estudo: REMOVIDO** (2026-08). `renderTarefas`/`renderTarefaEditor`/`renderTarefaTimer`/`renderTarefaImportPreview`, o parser de `.md` toggle (`parseTarefasToggleMd`), `K_TAREFAS`/`K_TAREFAS_HIST`, o ramo `"tarefa"` de `registrarConclusao` e as tools MCP `list_tasks`/`create_task`/`complete_task`/`update_task` saíram inteiros. Era uma tela escondida atrás do ☰ com editor, timer, importador e histórico próprios (~800 linhas) fazendo o que Metas + kanban do Diário já fazem. **Não recriar** — se voltar a fazer falta, o caminho é um tipo de cartão do kanban, não uma quinta superfície. `EIXOS` continua existindo (semeia as áreas da roda e as sugestões de área das metas).
- **Diário** (aba própria, `K_DIARIO`): um texto markdown por período, chaveado por `"dia:<ISO>"`, `"semana:<início configurado ISO>"`, `"mes:AAAA-MM"` ou `"ano:AAAA"` (`diarioChave`). A badge no topo troca o escopo e o input de data define o período. A tabbar tem 5 abas: Rotinas, Metas, Diário, Modelos, Configurações — ver a seção "Tabbar" abaixo para Dados/Configurações, que trocaram de lugar em 2026-08-23. O botão "agenda" (▤, `#diAgenda`) existe em **todos os quatro escopos**, não só no dia — arrastar a página para a direita também abre. Não existe mais um botão "+ bloco": digitar a linha de markdown é o único jeito de criar um item de agenda, em qualquer escopo.
  - **Escopo "dia"**: linhas de checklist que começam com horário (`- [ ] 08:00 - 09:30 Tarefa`, `RE_TIMEBLOCK`) viram blocos numa grade por minuto (`agendaHtml`) — time-blocking no estilo do day-planner do Obsidian. Bloco **sem hora de fim é sempre entendido como 1h fixa** (não "vai até o próximo bloco" — isso mudou; um horário solto não é um compromisso do tamanho que sobrar até o item seguinte). Blocos sobrepostos dividem a largura em colunas (`distribuirColunas`).
  - **Escopos "semana"/"mês"/"ano"**: markdown próprio por escopo, cada um com seu regex (`RE_WEEKBLOCK` — `- [ ] seg 08:00 Reunião` ou `- [ ] qui Tarefa`, hora opcional; `RE_MONTHBLOCK` — `- [ ] 05 Consulta`, dia do mês 1-31; `RE_YEARBLOCK` — `- [ ] mar Renovar CNH`, mês abreviado), renderizados como lista agrupada por dia/dia-do-mês/mês (`agendaGrupoHtml`), não como grade — não há granularidade de minuto nesses escopos. `agendaHtmlPara(escopo, texto, iso)` é o dispatcher (a grade do dia sai de `agendaGradeHtml`, compartilhada com a agenda inline da aba Rotinas); todos os quatro escopos compartilham o mesmo toque-para-concluir e **arrastar para a direita marca como adiado** (`attachSwipeAdiar`, `data-agline`), porque a linha de origem no texto é sempre identificada pelo mesmo índice independente do escopo. A ordem dos dias da semana em `agendaSemanaHtml` segue `ordemDiasSemana()` (dia de início configurável, ver seção da semana).
- **Estado "adiado" no markdown**: `RE_CHECK` aceita `[>]` além de `[ ]`/`[x]` (mesma convenção do plugin Tasks do Obsidian); `estadoCheck(m)` devolve `aberto|feito|adiado` e é o único lugar que interpreta o caractere. `setCheckStateInText()` alterna um estado específico, `toggleCheckLineInText()` continua alternando aberto↔feito.
- **Áreas das metas**: texto livre com sugestões (`mountTagInput`), não uma lista fixa. `EIXOS` entra como sugestão e empresta a cor; digitar um nome novo cria a área. `metaAreaInfo(v)` resolve tanto id de eixo legado quanto rótulo livre (cor derivada por hash do nome).
- **Modelos**: um tipo por entrada em `TMPL_TYPES`; adicionar um tipo novo exige tocar em 5 pontos — `TMPL_TYPES`, `newTemplateDoc()`, o mapa de `renderTemplateDoc()`, `tmplMeta()` e `docToMarkdown()`+`mdTypeLabel()`. A tela de pastas separa em duas seções: as fixas (uma por tipo) e as **anotações de rotinas** (uma pasta por rotina que gera journaling) — `templateFolderTiles()` devolve `{ fixas, rotinas }`.
- **Registro de pontos** (`type:"scoreboard"`): placar de jogo com `players[{id,name}]`, `rounds[{id,scores:{playerId:número}}]` e `higherWins`. Uma única grade CSS: colunas = jogadores, linha Σ de totais logo abaixo dos nomes, linhas seguintes = turnos. As colunas são `minmax(0,1fr)` e a tipografia encolhe por densidade (`.compacto` a partir de 4 jogadores, `.mini` a partir de 6) — até 8 jogadores cabem na largura do celular sem rolagem horizontal. Digitar um ponto **não** repinta a grade (o input perderia o foco) — só `atualizarTotais()` roda. Célula vazia é "ainda não jogou", diferente de zero.
- **Data de criação**: toda nota (`K_NOTES`) e todo documento de modelo carregam `createdAt` imutável, exibido por `criadoEmHtml()` logo abaixo do título (`tmplHeader` e `renderNoteEditor`). Itens antigos foram retroalimentados com `updatedAt` no boot.
- **Kanban por escopo do Diário** (`K_DIAKANBAN`, abaixo da anotação, em **todos** os escopos: dia, semana, mês e ano): array de `{id,text,col,per,ord,tagValor,eixo,gamItemId|gamPeriodo+gamPontos}` com colunas `todo|doing|done` (`DIA_KB_COLS`). O campo **`per`** é a chave do período no formato de `diarioChave` (`"dia:2026-07-27"`, `"mes:2026-07"`) — um campo só que carrega escopo + período e ordena por comparação de string dentro do mesmo escopo (`escopoDoPer`, `perAtual`). Nasceu como `dia` e é migrado por `migrarKanbanParaPeriodo`. Cartão novo já nasce com `tagValor:"baixo"` (não "nenhum" — pontuar por padrão, sem exigir abrir o cartão à toa), tanto no "+ item" inline do board (`pintarKanban`, só quando `opts.pesos`) quanto no lançamento rápido do player. `eixo` é a área da roda da vida do cartão, mesma convenção de `r.eixo`/`t.eixo` — selecionada pelo componente compartilhado `pintarAreaChips(wrap, atual, onChange)` (também usado no editor de rotina), só aparece com `rodaAtiva()`. `pontosCartao(tag, per, area)` e `registrarConclusao("kanban", {..., area})` respeitam a fatia da área **só para dia/semana** (que entram na semana); mês/ano viram bônus em `gam.metasPontos`, que não tem repartição por área.
  - **Peso**: `tagValor`, padrão `"nenhum"` (pontuar é opcional). O cartão **não tem duração** — vale um bloco da duração de referência, então `pesoBruto(tag, config.divisorDuracao)` = o próprio multiplicador. `pontosCartao(tag, per)` multiplica ainda por `KB_ESCOPO_MULT` (`dia:1, semana:2, mes:3, ano:4`): escopo mais longo pesa mais.
  - **Para onde vão os pontos**: dia e semana entram na nota da **semana** (`registrarConclusao("kanban", …)`, itemId **estável** `kanban:<cardId>:<ISO>` para poder estornar; o `mult` entra no próprio `pb` para `repararFatorSemanaAtual` não perder o escopo). Mês e ano entram em `gam.metasPontos[periodo]`, junto das metas — é lá que o fechamento mensal/anual lê. Sair de "Feito" ou excluir estorna (`descreditarCartao` cobre os dois caminhos). Tudo passa por `sincronizarPontosCartao`, chamado dentro de `gravarKanban` — o único ponto de reconciliação. Cartão em `done` fica **cravado no dia** em que foi concluído; cartão aberto ou em andamento é pendência e `rolarPendenciasPara(iso)` traz para hoje tudo que ficou atrás — e só roda quando `iso === localKey()`, para navegar pelo histórico não reescrever o passado. `gravarDiaKanban(cols, iso)` trata as colunas como a verdade e reconcilia (adota cartões novos, remove os que saíram, regrava `ord`). O tabuleiro é o mesmo do modelo "kanban": `pintarKanban(body, cols, gravar)` — **não** duplicar a lógica de arrasto. Variante `.kb-inline` para tela que já rola.
  - **Abas de coluna** (`opts.abas`, só no kanban do Diário): segmentado no topo e **uma coluna por vez** (`kbAbaAtiva`), para não obrigar a rolar a aba inteira até "Feito". Com uma só coluna no DOM, a posição do elemento deixa de ser o índice em `cols` — por isso cada `.kb-col` carrega `data-colidx` e o arrasto lê dali, nunca do índice do array de elementos.
  - **Horário no cartão** (`opts.horarios`, `it.hIni`/`it.hFim`, só no escopo dia): cartão com hora vira bloco na agenda por `blocosDoKanban(iso)`, concatenado aos blocos da nota dentro de `agendaHtml` (que reordena antes de `distribuirColunas`, pois ela assume a lista ordenada). Bloco de cartão usa `data-agcard` (e não `data-agline`, que é índice de linha do texto) e o clique conclui/reabre o cartão. Sem hora de fim = 1h, mesma regra da nota.
  - **O "✕" da edição do cartão CANCELA, não exclui** (excluir é a lixeira ao lado). Isso só é possível porque **nenhum campo é escrito no cartão enquanto a edição está aberta** — tudo é lido no "ok". Ao mexer nos campos de edição, manter essa regra.
  - Cartão em duas linhas: `.kb-card-top` (alça + texto) e `.kb-card-bot` (horário, peso, botões). Em linha única o texto ficava espremido entre a alça e três botões.
- **Nota anexada à rotina** (`r.notaId` → uma nota de `K_NOTES`): escolhida no editor da rotina (`#edNota`), aberta no player por `openPlayerNotaOverlay()` **sem interromper a execução** — o timer segue correndo atrás. Abre em leitura (`renderMdBlock`, com checklist e toggle clicáveis) e o lápis troca para **textarea**, não para o editor contínuo: só existe um `liveCtx` por vez e ele pertence ao Diário/Notas (mesma razão das anotações de journaling). Id órfão (nota apagada) só faz o botão sumir — `notaDaRotina()` devolve null, sem migração.
- **Nota por ETAPA** (`s.noteId` → uma nota de `K_NOTES`, diferente de `r.notaId` que é da rotina inteira): vinculada em `openStepEditor` (seção "Nota da etapa", só escrita em `s` no "Salvar", mesma convenção do resto do editor de etapa) — escolhe uma nota existente ou cria uma em branco na hora (título automático via `nomeAutoDoc()`, conteúdo preenchido depois em Modelos → Notas simples). No player, some o botão "&#8599; nota"/"ver nota vinculada" (zen e normal) que abre `openNoteOverlay(step.noteId)`, **somente leitura** — para editar o conteúdo é preciso ir em Notas simples. Útil para lembretes específicos da etapa (ex.: qual baralho de flashcard estudar). Nota apagada só some o botão, sem migração (mesmo padrão de `r.notaId`).
- **Nome automático de nota/documento novo**: `nomeAutoDoc()` → `"AA-MM-DD-HHHH"`, usado em `newTemplateDoc()` (todos os tipos) e em `openNoteEditor(null)`. Ordena por data no app e no Finder (o espelho markdown usa o título como nome de arquivo). O doc `countdown` é exceção proposital: é o container único das Metas, não uma nota.
- **"Não fazer" uma etapa** (`K_NAOFEITAS`, `{[routineId]:{date,ids}}`): encerra a etapa sem concluir e sem pontuar (`naoFazerEtapa()` → `advanceStep(true, true)`) e a anota como pendência do dia. Reabrir a rotina hoje traz **só as pendentes** (filtro em `startPlayer` + `podarDescansos`, que evita descanso solto nas pontas). Concluir de verdade limpa a marca; `goPrevStep` desfaz. Vira o dia, o registro é descartado no boot. Diferente de "pular etapa (opcional)", que descarta a etapa sem repescagem. `skipRowHtml`/`wireSkipRow` montam os dois botões — usados pelo player **e** pelo modo zen, para não divergirem.
- **Android nativo** (`android/app/src/main/java/com/pedro/rotinas/`): `ShortcutsPlugin` (atalhos do launcher), `TimerOverlayPlugin`+`TimerOverlayService` (bolha do cronômetro sobre outros apps), `RoutineWidgetProvider`+`RoutineWidgetConfigActivity`+`RoutineStore` (widget por rotina). O widget e a tela de configuração leem as rotinas direto de `filesDir/brita/rotinas_v2_routines.json` via `RoutineStore` — não passam pela ponte JS. Plugins novos precisam de `registerPlugin()` em `MainActivity`.
- **Bolha do cronômetro** (`TimerOverlayService`) — três regras que já foram bug e não devem regredir: (1) só aparece com o app em **segundo plano** (`appEmSegundoPlano`, alimentado por `appStateChange` do Capacitor + `visibilitychange`); o serviço sobe junto com a rotina e o extra `visible` esconde apenas a *view*, porque Android 12+ proíbe iniciar foreground service em segundo plano. (2) o JS manda a **fila** das etapas seguintes (`filaOverlay()`) e o serviço consome sozinho as automáticas (só descansos, mesmo critério de `catchUpSteps`) — sem isso a bolha ficava presa na etapa anterior contando negativo, porque o WebView suspenso não avança nada. (3) No plugin, ler millis com `call.getData().optLong(...)` e **não** `call.getLong(...)`: este último só devolve o valor se o parser JSON já entregou um `Long`, e `remainingMs` cabe em int → chegava como `Integer` e virava 0 (era o cronômetro zerado ao pausar).
- **Sons/vibração** — dois mecanismos independentes, cada um cobrindo um plano diferente:
  - **Primeiro plano**: `stepTransitionCue()`/`timeUpCue()`/`finishCue()`/`alarmCue()` (WebAudio `beep()` + `navigator.vibrate`), disparados de dentro do player (ex.: `startTicking`, `rem<=0`). Respeitam `soundMode(routine)` — `r.sound`: `"normal"|"suave"|"mudo"` — mas a vibração dispara **sempre**, mesmo em "mudo" (só o beep é que é cortado).
  - **Segundo plano** (`sincronizarAlertaFundo`, chamada por `sincronizarPlayerBackground` — o novo ponto único que substituiu as chamadas diretas a `sincronizarOverlay`): como o WebView suspenso não roda essas funções, o aviso de "tempo da etapa/descanso acabou" em segundo plano usa `LocalNotifications` (não a bolha — que só existe se a preferência `K_OVERLAY` estiver ligada E a permissão de sobreposição concedida). Por isso é **independente** da bolha: funciona bastando a permissão de notificação (mesma de `syncNativeSchedules`). Agenda uma notificação one-shot por transição, encadeando os descansos automáticos seguintes (mesmo critério de `filaOverlay()`/`auto`, teto de 10), sempre cancelando e reagendando do zero (`cancelarAlertaFundo`) a cada chamada — tag própria `extra.brita:"stepAlert"` para não colidir com o cancela-e-resync de `syncNativeSchedules()` (que só mexe em `"sched"`). Dois canais Android pré-criados no boot (`ensureTimerAlertChannels`): `brita_timer` (som+vibração, `r.sound !== "mudo"`) e `brita_timer_mudo` (`importance:2`, sem som, só vibra). `pararOverlay()` também cancela esses alertas, independente de ter bolha ou não — é o ponto único de "player parou".
  - As notificações agendadas de rotina (`syncNativeSchedules`, horário de início) e de meta recorrente (janela de lembrete) já tocam som+vibração **de graça**: usam o canal `"default"` do plugin `@capacitor/local-notifications`, que o próprio plugin cria com `setSound()` do som padrão do sistema e `Notification.DEFAULT_ALL` quando nenhum `sound` custom é passado — não precisou de canal próprio.

## Convenções de nomenclatura

- Funções de renderização de tela: `renderNomeDaTela` (camelCase, prefixo `render`).
- Constantes de chave de storage: `K_NOMEMAIUSCULO`.
- Atributos `data-*` em botões para lookup de handler (ex.: `data-edit`, `data-tvdel`, `data-mxup`) — seguir esse padrão para novas ações em vez de IDs únicos por elemento quando o botão é repetido em lista.

## Design tokens (identidade visual unificada, 2026-08-26 — ver skill pessoal `identidade-visual-apps`)

O rotinas segue o mesmo design system dos outros apps do Pedro (kuestions), migrado numa auditoria (Modo B da skill) e aplicado de uma vez. **Claro é o tema padrão** (inversão do que era antes — dark tinha sido a escolha original do rotinas); nomes de variável batem com os do kuestions. Já definidos em `:root`, `app.css`:

```css
--paper: #F5F5F8;     --card: #FFFFFF;       --card-2: #EDF1F6;
--line: #E3E2E8;      --ink: #1E1B26;        --sub: #5B6472;
--caneta: #6D28D9;    --caneta-2: #2F6BE0;   --caneta-soft: #F0E9FC;
--realce: #6D28D9;    --on-caneta: #FFFFFF;
--ok: #157A45;        --erro: #B23A2F;       --streak: #C2631A;
/* forma — sem sombra em lugar nenhum; superfície se distingue só por borda */
--radius: 16px;       --radius-sm: 8px;
--card-blur: ...;     /* fundo translúcido da tabbar (precisa de alfa) */
--heat-1..4: ...;     /* mapa de calor, rampa do próprio --caneta */
```

`--card-2`/`--caneta-2`/`--on-caneta`/`--card-blur` são tokens de apoio que o kuestions não define mas o rotinas precisa (surface secundária, um segundo tom funcional para links/kanban, texto sobre preenchimento sólido, blur da tabbar) — mantidos fora dos 8 nomes "oficiais" (`--paper/--ink/--caneta/--realce/--line/--sub/--ok/--erro`) só para não inventar conceito que o kuestions não tem.

Tema escuro é o override, via classe `body.dark` (mesmas variáveis, valores diferentes — nunca hardcodar cor, sempre `var(--x)`, para os dois temas continuarem funcionando). Ao mexer nos tokens de cor, atualizar também o `content` do `<meta name="theme-color">` em `applyTheme()`, o `<meta name="theme-color">` do `<head>`, `manifest.json` (`background_color`/`theme_color`) e `electron/main.js` (`backgroundColor` da janela) — todos precisam bater com `--paper` do tema ativo.

**Regras sem exceção**: ação primária é **sempre cor sólida** (`var(--caneta)`), nunca `linear-gradient(...)` — não existe mais `--grad` no app. Elevação é **só borda 1.5px** (`border:1.5px solid var(--line)`), nunca `box-shadow` decorativo — as duas exceções propositais são o indicador de posição do drag-and-drop (`.step-row.drop-above/below`, um truque de `box-shadow` sem blur pra desenhar uma linha sem reflow, não elevação) e as duas janelas "sempre no topo" que ficam por cima de qualquer app (`TimerOverlayService.java` no Android, `electron/mini-player.html` no desktop) — essas duas continuam fixas no tema escuro sempre, mesmo com o app no claro, porque precisam ficar legíveis sobre qualquer conteúdo atrás delas.

**Feedback de toque** com `transform: scale(...)` no `:active` (cards, `.icon-btn`, `.bell-btn`, `.play-btn`, `.btn-primary`, `.fab`) continua o padrão em componente novo. O check de concluir etapa é verde (`.ctrl-btn.ok`).

## Regras de layout que já foram reclamação

- **Tabelas (`.dev-row`)**: a 1ª coluna é texto e fica com toda a sobra (`flex:1 1 auto`); as colunas de número têm largura **mínima fixa**, `text-align:right` e `tabular-nums`, para uma linha alinhar com a de cima. O `space-between` sozinho esticava a coluna do meio e cada linha saía torta. `.dev-row.dev-head` é o cabeçalho, `.dev-n.wide` a 3ª coluna com texto longo. Não voltar a deixar as colunas com a mesma largura.
- **Cabeçalho de aba não é fixo em NENHUMA aba.** Metas e Modelos usam `.tab-scroll` (um nó `flex:1; overflow-y:auto` que leva o `.home-header` junto); em Metas o corpo é `#metasBody`, irmão do cabeçalho dentro do `.tab-scroll`, porque `paint()` monta os cards com `innerHTML +=`. Em Dados e Diário o título e a navegação ficam **dentro** da área que rola — numa tela de celular, fixá-los comia a altura útil. Padrão: um nó de rolagem com `#statsHead`/cabeçalho + um nó de conteúdo irmão (`#statsBody`, `#diContent`). O conteúdo é irmão, e não o mesmo nó, porque as seções são montadas com `innerHTML +=` — no mesmo nó cada seção nova recriaria os botões do cabeçalho e derrubaria os handlers.
- **Busca global no topo das abas**, no nível do título e do lado oposto: `cfgBtnHtml()` (só a lupa — a engrenagem saiu daqui, ver "Tabbar: Dados e Configurações trocaram de lugar" abaixo) no `.header-right` do `.home-header` + `wireCfgBtn(el)`. Não duplicar o botão nem o destino. A busca abre em overlay (`openGlobalSearch()`, classe `.confirm-overlay.search-overlay`) e não inline: assim a mesma superfície serve as abas sem nenhuma delas reservar espaço para campo e resultados — e o voltar do Android a fecha via `closeTopOverlay()`.
- **Tabbar: Dados e Configurações trocaram de lugar** (2026-08-23). A tabbar agora é Rotinas, Metas, Diário, Modelos, **Configurações** (`renderTabbar`, `data-tab="settings"` no lugar do antigo `"stats"`) — `renderSettings()` virou tela normal de aba (`class="screen with-tabbar"`, `renderTabbar("settings")` no fim, sem botão "← Voltar"), porque configurações são sobre o app inteiro, não sobre a aba em que você estava. O indicador de conflito de sync (`.sync-conflict-dot`/`data-syncdot`) foi junto, agora dentro do `.ic` do botão da tabbar (`.tabbar button .ic{position:relative}` ancora o ponto). **Dados saiu da tabbar**: só existe estatística de ROTINA ali, então virou um botão-ícone (`#statsBtn`, `ICON_D.stats`) no cabeçalho de Rotinas, abrindo `renderStats()` como subtela com `← Rotinas` (`class="screen"`, sem `renderTabbar`) — mesmo padrão de `renderRoutineDetail`/`renderBoletim`. `TECLA_ABA` (atalhos 1..5 do desktop) foi reordenado e `"5"` agora abre Configurações, não Dados. `view.tab` nunca é lido em lugar nenhum do código (só escrito) — cada `renderX()` chama `renderTabbar("<seu-próprio-nome>")` direto, então o campo é inerte; não se preocupar em mantê-lo "certo" ao navegar.
  - **Filtro de TIPO** (`GS_TIPOS`/`gsTipo`: tudo, rotinas, metas, notas, modelos, diário, kanban), escopo de sessão, não persistido — é um recorte para achar uma coisa agora. Metas entraram na busca junto com ele (antes não eram encontráveis de fora da aba).
  - **Ação inline no resultado** (`hit.acao = {label, title, fecha, run}`): rotina ganha ▶ (inicia na hora, `fecha:true`) e cartão do kanban ganha ✓/↺ (`fecha:false`, repinta a lista). A ação do cartão passa por `alternarCartaoFeitoNaAgenda`, a MESMA função da agenda do Diário — a pontuação é reconciliada igual, sem regra duplicada. O `e.stopPropagation()` no botão é obrigatório: o card inteiro navega.

## Componentes de botão já padronizados — reutilizar, não recriar

- `.icon-btn` — 34x34px, border-radius:var(--radius-sm), fundo var(--card), borda 1.5px var(--line), icone na cor var(--sub), font-size:15px. Este ja e o padrao unico para botoes so-de-icone.
- `.btn-primary` — acao principal (cor solida var(--caneta), nunca gradiente).
- `.btn-cancel` — cancelar (fundo neutro var(--card-2)).
- `.btn-confirm` — confirmar acao (geralmente em modais).
- `.btn-danger-outline` — acao destrutiva secundaria (outline vermelho).
- `.link-btn` — botao estilo link, sem fundo.

Icones sao caracteres Unicode via entidade HTML numerica (ex.: &#9998; = lapis, &#128465; = lixeira), nao sao SVGs nem arquivos de imagem, nem emoji digitado literalmente.

## Estado atual de features (para nao pedir de novo o que ja existe)

- OK Modelo "lista de mercado": gondolas sempre visiveis mesmo vazias ja implementado (comentario explicito no codigo, linhas ~3686-3687).
- OK Modelo "lista de viagem": ja reaproveita a estrutura da lista de mercado (comentario explicito, linha ~4607).
- OK Icone de editar por item de rotina: ja existe (data-stepedit, linha ~1219).
- OK Tarefas com bloco + tipo + conteudo + detalhes em bullets: ja implementado — tarefaDetalhesHtml() (linha ~2144) ja converte texto multilinha em lista com marcadores.
- OK Botoes de icone padronizados: classe .icon-btn ja unica e consistente em cor/tamanho/estilo.
- OK Espaco abaixo dos cards fixados da home (rotina em andamento, semana fechada): classe `.pinned-card`.
- OK **Cabecalho da aba Rotinas** (reorganizado em 2026-08): titulo -> card da roda da vida -> slot `#homeSemanaFechada` -> linha `hoje`/`semana`/`dia` -> avisos -> conteudo (lista de rotinas OU uma das duas agendas, ver a secao "Agenda inline"). **Sairam**: o botao-icone de boletim (o card da roda leva la), o filtro multiselect de area (`areaFiltroChipsHtml`/`wireAreaFiltroChips`/`passaFiltroArea`/`K_HOMEAREA`, todos removidos — o filtro de Metas e o do kanban do Diario continuam existindo e sao outros) e o card "Hoje" (compromissos + kanban do dia). `rolarPendenciasPara`/`gerarCartoesRecorrentesHoje` **continuam sendo chamadas em `renderHome`** mesmo sem o card: elas alimentam o Diario e precisam rodar no primeiro render do dia.
- OK **Card "Roda da vida — semana" absorveu o card "Boletim da semana"**: `rodaVidaResumoHtml()` monta as barras por area MAIS um rodape `.roda-boletim` com `ritmoInfo()` (nota/ritmo/dias restantes), e o card inteiro leva a tela de boletim. O clique e **delegado no document** por `[data-boletimcard]`, uma vez so — o card e remontado por `render()` em quatro telas, duas delas com `innerHTML +=`, entao religar `.onclick` a cada pintura seriam quatro pontos para esquecer. O card so deixa de existir quando nao ha nem area pontuada nem `totalBrutoAgendado > 0`.
- OK **Rotina concluida hoje desce para o fim da lista** e troca o chip de horario planejado pelo REAL da execucao (`execucaoDeHoje(id)` + `horarioRealChipHtml(h)`, chip verde com check). Vale tambem para rotina sem horario planejado, que antes nao tinha chip nenhum. O historico passou a gravar **`startedTs`** em `finishRoutine`; execucao antiga sem esse campo tem o inicio reconstruido de `ts - (actualSec + pausedSec)`. A ordenacao e um `sort` estavel sobre `rotinasOrdenadas()`, entao a ordem por horario se preserva dentro de cada grupo.
- OK **Streak no card da rotina e uma tag** (`.streak-tag`, estilo Duolingo): borda e texto em `var(--streak)` (laranja, com override proprio no tema claro) + icone `ICON_D.fire` + o numero de dias. Nao voltar ao `· ●3d` solto dentro da linha de metadados.
- OK Lista de rotinas sempre ordenada pelo horario de inicio: `rotinasOrdenadas()` (sem agendamento vai para o fim). A home nao tem reordenacao manual — o `.drag-handle` de `.routine-card` e das ETAPAS no editor, nao das rotinas.
- OK Toggle "so hoje" na aba Rotinas (`K_SOHOJE`, persistido; `.bell-btn.on` e o estado ligado): filtra por **`rotinaCabeEmHoje(r)`** = `rotinaOcorreHoje(r) || rotinaSemDiaFixo(r)`. Rotina sem agendamento ativo nao tem dia devido, mas tambem nunca esta fora de dia — antes ela sumia justamente no modo em que se quer saber o que ainda da pra fazer. **`rotinaOcorreHoje` continua estrito** (nudge, MCP, notificacoes querem so o que tem dia devido) — nao mexer nele, usar `rotinaCabeEmHoje` so onde a intencao for "o que cabe hoje".
- OK Dias da rotina aparecem em chip proprio ABAIXO da linha de horario (`diasChipHtml()` → "ter/qui", cor `--sub`), nao mais resumidos em "2d/sem" ao lado do horario. Rotina de todo dia nao gera chip. Vale na home e no detalhe da rotina.
- OK Lancamento rapido do player tem 4 opcoes: nota simples (cria/edita em `K_NOTES`, mesmo padrao de escolher-existente-ou-criar que a antiga "ideia" tinha), despesa, nota do dia (anexa em `K_DIARIO`) e cartao em "A fazer" (`K_DIAKANBAN`, com peso). O modelo de nota "Ideia" (TMPL_TYPES) foi removido do app — nao recriar.
- OK Player tem painel ☰ com TODAS as etapas (`openPlayerStepsOverlay`), com reordenacao por ↑/↓. So libera mover da etapa ATUAL em diante: `stepActuals` e indexado por posicao e as posicoes ja executadas guardam o que aconteceu — mesma invariante de `adiarEtapa`. Reordenar chama `render()` + `savePlayerSnapshot(true)` + `sincronizarOverlay()` (a bolha nativa consome a fila das proximas etapas).
- OK Meta tem anotacao markdown propria (`t.nota`, `abrirNotaMeta`), renderizada no card por `renderMdBlock` com checklist e toggle clicaveis.
- OK Kanban: os botoes de mover cartao sao `.kb-move-btn` (30x30, alvo de 44px por `::after`); o salto para a ULTIMA coluna vira um check verde (`.para-feito`). Nao voltar aos `.order-btn` de texto.
- OK Paisagem: bloco `@media (orientation: landscape) and (max-height: 560px)` no fim do `<style>` — mostrador do player medido em `vh`, kanban em 3 colunas lado a lado, tabbar e grades comprimidas. O recorte por `max-height` deixa tablet deitado com o layout normal.
- OK **Desktop** (`@media (min-width: 900px)`, ultimo bloco do `<style>`): a MESMA tabbar vira sidebar de 196px (`flex-direction:column`; precisa de `flex:0 0 auto` nos botoes, senao o `flex:1` do mobile divide a ALTURA e espalha as 5 abas pela tela), cada `.home-header` vira topbar `sticky`, listas de Rotinas/Metas em grade de 2 colunas, tiles de Modelos sem `aspect-ratio`, FAB vira pilula rotulada por `content:attr(title)` (por isso todo FAB novo precisa de `title`), `:hover` sob `@media (any-hover:hover)`, e a `.md-toolbar` (barra do teclado virtual) fica escondida. **Quase tudo e CSS pura** — o unico ponto que consulta largura em JS e `ehDesktop()`/`DESKTOP_MQ`, usado so pelo Diario; qualquer outro ajuste de desktop deve ir para o CSS.
  - **Diario em 3 colunas** (agenda | anotacao | kanban, `.di-cols`/`.di-pane`): no celular a agenda e um TOGGLE que substitui a nota, no desktop as tres convivem — e estrutura diferente, nao estilo, por isso mora em `renderDiario.paint()` e nao no CSS. Cada painel rola sozinho (`.di-pane-body`); `#diBody` perde o scroll proprio para nao criar duas barras. Com as duas visiveis ao mesmo tempo, a agenda precisa acompanhar o que esta sendo digitado: `diarioLiveCtx().setText` chama `agendarRepinturaDaAgenda()` (debounce 220ms) → `repintarAgendaDoDiario()`, que reescreve **so** `#diAgendaPane` e religa `wireAgendaBlocos()`. Repintar via `paint()` inteiro recriaria o `contenteditable` e derrubaria o cursor no meio da digitacao. O hook e zerado em `render()` (aponta para nos da tela anterior) e declarado no topo do IIFE por causa do TDZ.
  - `matchMedia` **falta no jsdom** dos testes: `DESKTOP_MQ` e criado sob `typeof window.matchMedia === "function"`, senao o boot inteiro morre no `npm test`.
- OK Atalhos de teclado (`TECLA_ABA`): 1..5 trocam de aba, `/` abre a busca global, Esc fecha overlay/volta. `digitandoAgora()` e obrigatorio antes de qualquer atalho — o editor continuo e um `contenteditable`, entao sem a guarda um "1" no meio de uma frase trocaria de aba. Modificadores (Cmd/Ctrl/Alt) sao ignorados para nao roubar atalhos do navegador.
- OK **Pill "Notas"/"Outros" da aba Modelos** usa `.type-toggle.view-toggle` — o MESMO componente do seletor semanal/mensal/anual de Dados, e nao os chips de area (`.area-chip`): sao duas VIEWS excludentes da mesma aba, que e o que esse toggle representa no resto do app. `modelosTabPillHtml(active)`/`wireModelosTabPill(scope)`, compartilhados por `renderTemplates` e `renderNotes`.
- OK **Cabecalho da aba Dados** (agora subtela de Rotinas, não aba própria — ver "Tabbar" acima): backup, importar, relatorio (botao-icone `ICON_D.clipboard`). **Sairam** o botao "CSV" e a funcao `exportStatsCsv` inteira (o botao era o unico ponto de entrada), a lupa e a engrenagem (a subtela nao usa mais `cfgBtnHtml`/`wireCfgBtn`).
- OK Nos blocos de time-blocking do Diario o nome fica na mesma linha do horario (`.ag-bloco` em `row wrap`; `.ag-txt` com `flex:1 1 0` — com basis `auto` o texto largo quebrava para baixo de novo).
- OK Aviso de ritmo tem escolha de dias em Configuracoes (`K_NUDGEDAYS`, padrao sexta; `K_NUDGEDONE` marca `"<semana>:<dia>"` para disparar uma vez por dia escolhido).
- OK O botao de agenda do Diario tem texto ("agenda"), existe nos 4 escopos, dentro de `#diContent` — religar em `wireAgendaToggle()` a cada `paint()`. Nao ha mais botao "+ bloco" em nenhum escopo.
- OK **Agenda inline da aba Rotinas** (2026-08-23; substituiu a tela `renderAgenda`/`view.screen==="agenda"`, que listava so rotinas com horario e cujo clique abria o EDITOR). `homeView` (`K_HOMEVIEW`, persistido: `rotinas|semana|dia`) decide o que ocupa a area de conteudo da aba; os botoes `semana`/`dia` sao toggles (tocar no ativo volta para a lista) e o botao `hoje` so aparece na lista, onde o filtro tem sentido.
  - **`itensAgendaDoDia(iso)`** e a fonte unica das duas visoes: rotinas agendadas (`rotinaAgendadaEm` + `computeSchedule`) + cartoes do kanban daquele dia + compromissos avulsos, numa lista so. Rotina ja executada usa o horario **REAL** (`execucaoDoDia` + `execucaoMinutos`, o mesmo par que alimenta `horarioRealChipHtml`), o que tambem muda a **posicao** dela na ordem.
  - **"semana"**: proximos 7 dias em lista (`agendaSemanaHomeHtml`), um `+` por dia. Clique na linha: rotina -> `routineDetail` (de onde o "Comecar" fica a um toque, nunca mais o editor); tarefa -> `alternarCartaoFeitoNaAgenda` (cinza + tachado); compromisso -> alterna `c.feito`. O lapis so existe na tarefa, com `stopPropagation` (a linha inteira alterna concluido).
  - **"dia"**: `agendaDiaHomeHtml(hoje)` — a mesma grade do Diario, mas com a janela fixa em 00:00–24:00, marcacao de 30 em 30 (`.ag-hora.ag-meia`) e o **dobro** do zoom (`AG_PX_MIN_ZOOM`), porque a maioria das tarefas dura de 15min a 1h. Rola dentro de `.ag-dia-scroll` (a grade inteira tem ~3300px) e abre ja na altura de agora. Alem dos blocos do Diario (nota, kanban, compromisso, iCal) leva as **rotinas** (`b.rot`/`data-agrotina`, classe `.ag-rot`).
  - A grade e a MESMA funcao das duas telas: `agendaGradeHtml(blocos, iso, opts)` (janela `mIni`/`mFim`, zoom `pxMin`, `passo` entre marcacoes) — `agendaHtml` do Diario e so a chamada com o padrao. Nao duplicar a grade.
  - **`abrirPopupTarefa(iso, cardId)`**: os mesmos campos da edicao de cartao do kanban do Diario (texto, horario, peso, area) **menos** o "repetir toda semana", gravando no MESMO `diaKanban` — a tarefa criada aqui aparece no kanban e na agenda do Diario e pontua por `sincronizarPontosCartao`, sem regra duplicada. Cartao ja concluido que muda de peso/area estorna antes de recreditar.
  - "pausar agenda" (`snoozes`) mora no topo da visao "semana" (`abrirSnoozeModal`), que era o unico conteudo da tela removida que nao tinha outro lugar.
  - **Marcador antes do nome**: bolinha colorida (`.r-dot`, cor da rotina) para rotina, **quadradinho neutro** (`.ag-square`, `var(--sub)`) para tarefa/compromisso — distingue "programado" de "adicionado avulso" sem texto. Coluna de horario (`.agenda-time`) e o slot do lapis (`.agenda-edit-slot`, mesma largura do botao mesmo quando ele nao existe) tem **largura fixa em CSS**, nao `flex:auto`: e o que faz as linhas dos 7 dias alinharem feito tabela mesmo com "sem hora"/"18:00"/"07:00–07:40" tendo tamanhos de texto diferentes.
- OK **`.icon-btn.borderless`** (`app.css`, 2026-08-23): variante sem fundo/borda/sombra do `.icon-btn` padrao, para botao-icone que fica melhor "flutuando" sobre o card em vez de num quadrado proprio — usado no excluir da area da roda da vida (com `ICON_D.trash` vermelho, no lugar do antigo `xmark`), no editar/+ da agenda inline de Rotinas, no favoritar de nota (`.pin-btn`) e no editar de meta. So remove o estilo visual — o alvo de toque continua 34x34. Reutilizar em vez de inventar outro botao sem borda.
- OK **Pomodoro e "ciclo de repeticao" SAIRAM do editor de rotina** (2026-08-23), junto com `applyCycle` — `playbackSteps` agora e so `expandSteps` + descansos. Rotina antiga com `r.cycle` guardado nao repete mais nada (o campo fica inerte). Nao recriar: pomodoro era um gerador de etapas que o "+ adicionar etapa" ja cobre, e o ciclo era um segundo eixo de repeticao invisivel no player.
- OK **Roda da vida em Configuracoes: lista de areas simplificada** (2026-08-23). Saem os botoes de mover para cima/baixo (reordenar area nao era usado o bastante pra pagar duas colunas de botao); **nome da area agora e editavel** direto na lista (`data-rodalabel`, grava em `a.label` no `onchange`, nunca vazio). Excluir virou `.icon-btn.borderless` com `ICON_D.trash` vermelho no lugar do `xmark` num quadrado. Com os botoes de mover fora, cor/nome/peso/excluir sao os 4 campos de largura fixa que alinham como tabela entre todas as areas (`.roda-area-row`, `.roda-area-nome` flexivel, `.roda-area-peso` 44px, botao 34px) — nao reintroduzir um campo de largura variavel entre eles.
- OK **Metas: botao de excluir do card ficou so no swipe** (2026-08-23). O card mostra so o editar (`.icon-btn.borderless`); o "✕" antigo (`data-cddel`) continua no DOM com `display:none` porque o wiring do swipe-para-excluir em `renderMetas` acha o card **pelo proprio botao** (`card.querySelector("[data-cddel]")`) e clica nele por proxy — apagar o elemento quebraria o swipe junto. Se algum dia o wiring for reescrito para nao depender do botao, ai sim da pra remover o `<button>` de verdade.
- OK **Modelos: pill Notas/Outros com fundo proprio** (2026-08-23, `.modelos-tab-pill` em `app.css`) — o `.type-toggle` generico (fundo `--card-2`) quase sumia direto no `.home-header` sem um card ao redor; esta instancia ganhou borda+mais padding vertical (sem sombra, como o resto do app), sem mexer no componente generico usado em pesos/toggles no resto do app. **Aba padrao ao abrir Modelos e "Notas"**, nao "Outros" — o clique na tabbar de `data-tab="templates"` abre direto `screen:"notes"` (unico caso especial no handler generico de clique da tabbar).
- OK Dados por rotina tem a secao "Planejado - real (media . mediana)": 1a coluna nome, 2a `plan - media`, 3a `plan - mediana` (sinal invertido em relacao a secao de desvio mediano logo acima; positivo = sobrou tempo).
- OK Roda da vida (`gam.config.roda`), habito consolidado (`gam.config.habito`) e vagas excludentes por peso (`gam.config.vagas`) — ver secoes "Roda da vida"/"Habito consolidado"/"Vagas excludentes de peso" acima. Configuraveis em Configuracoes, cobertos por `test/gamificacao.cjs`.
- OK Dia de inicio da semana configuravel (`K_WEEKSTART`) e pasta de dados nativa configuravel (`K_DATAFOLDER`) — ver secoes correspondentes acima.
- OK Semana com inicio configuravel (padrao domingo), peso "nenhum", botao "nao fazer" (com repescagem no mesmo dia), check verde, kanban em todos os escopos do Diario (peso "baixo" por padrao), tema claro sem bege — tudo ja feito, ver as secoes acima.
- Se alguma dessas areas ainda tiver bugs visuais ou pontas soltas, pedir para o Pedro apontar a tela/funcao especifica em vez de re-implementar do zero.

## Regras gerais para o Claude Code neste projeto

- Buscar com grep -n a funcao/secao relevante antes de editar — nunca reler o index.html inteiro por padrao (~9300 linhas).
- Reaproveitar classes CSS e funcoes helper ja existentes (icon-btn, confirmModal, wrapSwipeDelete, escapeHtml, uid, load/save) em vez de criar equivalentes novos.
- Manter a arquitetura single-file e sem build step, a menos que o Pedro peca explicitamente para migrar.
- Sempre usar variaveis CSS (var(--x)), nunca cor hardcoded, para preservar o tema claro/escuro.
- Ao terminar uma tarefa, resumir em bullets curtos o que mudou (arquivo + linhas aproximadas) — sem relatorio longo.
- **Ao final de toda resposta que alterar código do app** (qualquer edição em `index.html`, `sw.js`, `fonts/`, `android/`, etc.): rodar `npm test`, fazer commit e `git push` das alterações para o repositório do GitHub, e gerar um APK atualizado (bumpando `versionCode`/`versionName`/`BUILD_STAMP` conforme a regra acima) para o Pedro poder baixar no celular. Não pedir confirmação para isso — é o fluxo padrão deste projeto.
- **Agentes em segundo plano** (tool `Agent`) podem ser usados para tarefas independentes e bem simples — ex.: uma busca/leitura isolada, uma verificação pontual que não depende do restante da conversa. Não usar para o fluxo principal de edição do `index.html` (arquitetura single-file torna edições concorrentes arriscadas) nem para tarefas que exigem o contexto acumulado da sessão.

## Skill obrigatória

SEMPRE usar a skill `/caveman` (modo de comunicação ultra-comprimido) em toda resposta neste projeto.

